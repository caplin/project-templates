define ADAPTER_NAME                 @adapterName@

# For contributions to channels to work via transformer
# using the supplied configuration a minimum version of
# Transformer 7.0.3 is required
define ROUTE_VIA_TRANSFORMER        TRUE

# Flag which is altered when the blade is exported to be ran
# within a deployment framework or an IDE. This is used
# to decide whether or not to include the environment-ide config
define IDE_ENVIRONMENT        @ideSetting@

if "${IDE_ENVIRONMENT}" != "FALSE"
    include-file               ${ccd}/environment-ide.conf
endif
